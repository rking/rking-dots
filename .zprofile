#XXX this is causing a loop on my current install. Need to figure out the
# solution for all machines.
#[ "$TERM" = screen.linux ] && export TERM=screen
#(screen -list | grep -C100 'Multi, .*tached') || exec screen
#screen -r
